<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class ProductImageSeeder extends Seeder
{
    public function run(): void
    {
        // The repository has no verified automotive image fixtures. Keep image
        // records empty rather than seeding broken Fashion paths or remote URLs.
    }
}
